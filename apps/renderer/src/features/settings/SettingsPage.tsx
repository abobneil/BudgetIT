import { FeaturePlaceholderPage } from "../common/FeaturePlaceholderPage";

export function SettingsPage() {
  return (
    <FeaturePlaceholderPage
      title="Settings"
      description="Dedicated settings center for runtime, notifications, backup/restore, security, and maintenance is planned in U13."
    />
  );
}

